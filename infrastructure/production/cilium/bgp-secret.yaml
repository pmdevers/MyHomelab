---
# BGP MD5 auth password matching the Ubiquiti FRR config.
# Keep this secret out of git — apply manually or via external-secrets.
apiVersion: v1
kind: Secret
metadata:
  name: bgp-peer-password
  namespace: kube-system
type: Opaque
stringData:
  password: "hunter2"
