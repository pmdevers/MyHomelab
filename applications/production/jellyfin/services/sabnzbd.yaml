apiVersion: v1
kind: Service
metadata:
  name: sabnzbd
  namespace: jellyfin
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: sabnzbd
    app.kubernetes.io/part-of: jellyfin
