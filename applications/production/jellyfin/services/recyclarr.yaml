apiVersion: v1
kind: Service
metadata:
  name: recyclarr
  namespace: jellyfin
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      name: http
  selector:
    app: recyclarr
    app.kubernetes.io/part-of: jellyfin
