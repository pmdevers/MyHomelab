apiVersion: v1
kind: Service
metadata:
  name: sonarr
  namespace: jellyfin
spec:
  type: ClusterIP
  ports:
    - port: 8989
      targetPort: 8989
      name: http
  selector:
    app: sonarr
    app.kubernetes.io/part-of: jellyfin
